# aulapwa_2026

Este projeto é um PWA simples chamado Plênio.

- Abra `index.html` em um servidor ou pelo GitHub Pages.
- O app registra um service worker em `sw.js`.
- O `manifest.json` habilita instalação no navegador.
- Use `styles.css` para o layout responsivo e visual.
